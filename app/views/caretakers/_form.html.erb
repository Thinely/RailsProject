<%= form_with(model: caretaker) do |form| %>
  <% if caretaker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(caretaker.errors.count, "error") %> prohibited this caretaker from being saved:</h2>

      <ul>
        <% caretaker.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class:"form-control col-6" %>
  </div>

  <div class="field form-group">
    <%= form.label :contact_no %>
    <%= form.number_field :contact_no, class:"form-control col-6" %>
  </div>

  <div class="field form-group">
    <%= form.label :house_id %>
    <%= form.collection_select(:house_id, @house_options,:id,:building_no,  { class: 'form-control col-6' }) %>

  </div>

  <input type="submit" class="btn btn-primary" value="Submit">

<% end %>
