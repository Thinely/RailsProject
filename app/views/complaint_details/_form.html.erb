<%= form_with(model: complaint_detail) do |form| %>
  <% if complaint_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(complaint_detail.errors.count, "error") %> prohibited this complaint_detail from being saved:</h2>

      <ul>
        <% complaint_detail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tenant_id %>
    <%= form.number_field :tenant_id %>
  </div>

  <div class="field">
    <%= form.label :complaint_details %>
    <%= form.text_field :complaint_details %>
  </div>

  <div class="field">
    <%= form.label :complaint_date %>
    <%= form.datetime_select :complaint_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
